# Contributing to cpointed

- **Authoring:** Sign commits and code comments per project convention (`# Made by Sn0w8ird` in Python).
- **Ethics:** Only contribute capabilities intended for authorized research and defense.
- **Tests:** `pytest` from the repository root after `pip install -e ".[dev]"`.
